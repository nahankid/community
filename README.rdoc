When trying to view Groups in ActiveAdmin, this is throwing error:

undefined method `__sort_option__' for :ancestry:Symbol

This doesn't work with the combination:
Mongoid, mongoid-ancestry and active_admin-sortable_tree


But works fine with ActiveRecord, Ancestry and active_admin-sortable_tree